import {IsAuthUserDirective} from './auth';
import {FormattableDateDirective} from './FormattableDate';

export const schemaDirectives = {
  isAuthUser: IsAuthUserDirective,
  formatDate: FormattableDateDirective
};
